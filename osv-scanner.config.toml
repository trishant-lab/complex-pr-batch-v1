[[IgnoredVulns]]
id = "PYSEC-2026-3447"
reason = "setuptools 83.0.0 fix removes pkg_resources, required at runtime by supervisor==4.2.5, novu==1.14.0, and clearml (none have a fixed release without pkg_resources). Build-time sdist/MANIFEST.in issue not exploitable for this deployed app. Revisit if these deps drop pkg_resources."
