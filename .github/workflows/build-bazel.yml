name: "Build Bazel Launchpad App"

on:
  workflow_dispatch:


jobs:
  deploy:
    name: Build Bazel Launchpad App
    runs-on: [self-hosted, python3.9]
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Bazel Build
        run: bazel build //...
