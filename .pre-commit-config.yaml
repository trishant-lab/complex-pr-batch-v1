default_stages:
  - pre-commit
repos:
  - repo: 'https://github.com/charliermarsh/ruff-pre-commit'
    rev: v0.9.10
    hooks:
      - id: ruff-format
      - id: ruff
  - repo: 'https://github.com/PyCQA/bandit'
    rev: 1.7.9
    hooks:
      - id: bandit
        args:
          - '-c'
          - pyproject.toml
          - '-r'
          - '-ll'
        additional_dependencies:
          - '.[toml]'
  - repo: local
    hooks:
      - id: osv-scanner
        name: osv-scanner
        entry: osv-scanner --lockfile=uv.lock --config=osv-scanner.config.toml
        language: system
        always_run: true
        pass_filenames: false
      - id: pre-commit-checks
        name: pre-commit-checks
        entry: python app/pre_commit_checks.py
        language: system
        always_run: true
        pass_filenames: false
        types: [python]
  - repo: https://github.com/gitleaks/gitleaks
    rev: v8.16.1
    hooks:
      - id: gitleaks