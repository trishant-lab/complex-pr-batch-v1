{
  "name": "dexit-document-export",
  "workflowId": "dexit-document-export",
  "description": "Notifies a user when their document export completes or fails. The message text is composed by dexit-app, so the template only renders it.",
  "tags": ["document-export"],
  "active": true,
  "__source": "dashboard",
  "steps": [
    {
      "name": "In-App",
      "type": "in_app",
      "stepId": "in-app-step",
      "controlValues": {
        "body": "{{payload.document_export.message}}",
        "redirect": { "url": "/documents/exports", "target": "_self" }
      }
    }
  ],
  "preferences": {
    "workflow": {
      "all": { "enabled": true, "readOnly": false },
      "channels": {
        "in_app": { "enabled": true },
        "email": { "enabled": false },
        "chat": { "enabled": false },
        "sms": { "enabled": false },
        "push": { "enabled": false }
      }
    },
    "user": null
  }
}
