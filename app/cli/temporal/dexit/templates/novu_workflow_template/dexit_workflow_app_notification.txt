{
  "name": "dexit-workflow-app-notification",
  "workflowId": "dexit-workflow-app-notification",
  "description": "Generic in-app message raised by a notification step in the workflow builder. The text is composed by the workflow, so the template only renders it.",
  "tags": [],
  "active": true,
  "__source": "dashboard",
  "steps": [
    {
      "name": "In-App",
      "type": "in_app",
      "stepId": "in-app-step",
      "controlValues": {
        "body": "{{payload.workflow.content}}"
      }
    }
  ],
  "preferences": {
    "workflow": {
      "all": { "enabled": true, "readOnly": false },
      "channels": {
        "in_app": { "enabled": true },
        "email": { "enabled": false },
        "chat": { "enabled": false },
        "sms": { "enabled": false },
        "push": { "enabled": false }
      }
    },
    "user": null
  }
}
